{{- if .Values.agent.enabled }}
---
{{ include "otterscale-agent-flux.helmrelease" (dict "root" $ "release" .Values.agent) }}
{{- end }}
{{- if .Values.flux.enabled }}
---
{{ include "otterscale-agent-flux.helmrelease" (dict "root" $ "release" .Values.flux) }}
{{- end }}
