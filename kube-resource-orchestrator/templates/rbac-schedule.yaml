apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
  name: kro-schedule-admin-role
rules:
  - apiGroups:
      - kro.run
    resources:
      - schedules
    verbs:
      - "*"
  - apiGroups:
      - kro.run
    resources:
      - schedules/status
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
  name: kro-schedule-editor-role
rules:
  - apiGroups:
      - kro.run
    resources:
      - schedules
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  - apiGroups:
      - kro.run
    resources:
      - schedules/status
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    rbac.authorization.k8s.io/aggregate-to-view: "true"
  name: kro-schedule-viewer-role
rules:
  - apiGroups:
      - kro.run
    resources:
      - schedules
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - kro.run
    resources:
      - schedules/status
    verbs:
      - get
